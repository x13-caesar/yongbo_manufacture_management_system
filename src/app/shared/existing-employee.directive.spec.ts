import { ExistingEmployeeDirective } from './existing-employee.directive';

describe('ExistingEmployeeDirective', () => {
  it('should create an instance', () => {
    const directive = new ExistingEmployeeDirective();
    expect(directive).toBeTruthy();
  });
});
